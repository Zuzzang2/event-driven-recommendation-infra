apiVersion: v1
kind: Service
metadata:
  name: recommender
  labels:
    app: recommender
spec:
  type: ClusterIP
  selector:
    app: recommender
  ports:
    - name: http
      port: 8000
      targetPort: http
