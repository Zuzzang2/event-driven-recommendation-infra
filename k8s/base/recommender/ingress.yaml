apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: recommender
  labels:
    app: recommender
spec:
  ingressClassName: traefik
  rules:
    - host: predict.43.200.40.173.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: recommender
                port:
                  number: 8000
